tests:
  services:
    - postgres:9.5
  script:
    - pip install -e .
    - ./setupdb.sh .env.test
    - nosetests tests
  tags: ["angr"]

# lint:
#   script: "lint.py"
#   tags: ["cgc-linter"]

docs:
  script: "generate_docs"
  tags: ["cgc-linter"]
