tests:
  services:
    - postgres:9.5
  script:
    - pip install -e .
    - ./setupdb.sh .env.test
    - nosetests tests
  tags: ["angr"]

# lint:
#   script: "lint.py"
#   tags: ["cgc-linter"]

docs:
  script: "generate_docs"
  tags: ["cgc-linter"]

build_images:
    only: ["master"]
    stage: deploy
    script:
    # - angr-build angr
    # - angr-build angr-ci
    - angr-build worker
    - angr-build meister
    tags: ["docker-builder"]
